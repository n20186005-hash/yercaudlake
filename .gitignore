# Dependencies (not uploaded; install on the deploy machine)
node_modules/

# Build output (generated by `astro build`, then deployed)
dist/
.astro/

# Cloudflare Workers build / secrets
.wrangler/
.dev.vars
.env
.env.*
!.env.example

# Logs & CI / audit artifacts (not part of the site)
*.log
ci-install.log
CI-RESULT.txt
source-audit.log

# OS / editor
.DS_Store
Thumbs.db
